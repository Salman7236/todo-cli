[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "todo-cli"
version = "0.1.0"
dependencies = [
    "typer",
    "rich",
]

[project.scripts]
todo = "main:app"
